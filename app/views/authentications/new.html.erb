<%= form_for @user, {url:"/authentications", method:"post"} do |u| %>
  <%= u.text_field :name, placeholder: 'Login'%>
  <%= u.text_field :password, placeholder: 'Password'%>

  <%= u.submit "Sign in" %>
<% end %>