
<%= form_for [@user, @graph], {url: user_graphs_url} do |form| %>
  <%= form.text_field :name, placeholder: 'Graph Name'%>
  <%= form.text_field :data, placeholder: 'File Path'%>

  <%= form.submit "Create Graph" %>
<% end %>